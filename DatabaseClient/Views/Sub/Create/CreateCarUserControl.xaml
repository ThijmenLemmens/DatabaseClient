<UserControl x:Class="DatabaseClient.Views.Sub.Create.CreateCarUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="280">
    <Border Background="#FFE5E5E5" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Car Company Dropdown -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Car Company:" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="1" Height="30"
                          ItemsSource="{Binding CarCompanies}"
                          SelectedItem="{Binding SelectedCarCompany}"
                          DisplayMemberPath="Name"/>
            </Grid>

            <!-- Name Field -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Name:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Height="25" FontSize="14" Text="{Binding Name, Mode=TwoWay}"/>
            </Grid>

            <!-- PK / KW Field -->
            <Grid Grid.Row="2" Margin="0,0,0,10">
                <StackPanel Orientation="Vertical" Grid.ColumnSpan="2">
                    <!-- Radio Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                        <RadioButton Content="Use PK" IsChecked="{Binding IsPkSelected, Mode=TwoWay}" GroupName="PowerType" Margin="5,0"/>
                        <RadioButton Content="Use KW" IsChecked="{Binding IsKwSelected, Mode=TwoWay}" GroupName="PowerType" Margin="15,0"/>
                    </StackPanel>

                    <!-- Power Input -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Power:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Height="25" FontSize="14" 
                                 Text="{Binding PK, Mode=TwoWay}" />
                    </Grid>
                </StackPanel>
            </Grid>

            <!-- Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Create" Width="100" Margin="10,0"
                        Command="{Binding CreateCommand}"/>
                <Button Content="Reset" Width="100" Margin="10,0"
                        Command="{Binding ResetCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
